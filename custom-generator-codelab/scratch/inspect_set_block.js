import * as Blockly from 'blockly/core';
import 'blockly/blocks.js';

console.log("lists_setIndex init:", Blockly.Blocks['lists_setIndex'].init.toString());
console.log("lists_setIndex keys:", Object.keys(Blockly.Blocks['lists_setIndex']));
